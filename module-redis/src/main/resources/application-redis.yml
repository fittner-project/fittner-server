spring:
  data:
    redis:
      host: api.fittner.co.kr
      port: 6379
      password: ${REDIS_PASSWORD}

---
spring.config.activate.on-profile: local
spring:
  data:
    redis:
      password: ${REDIS_PASSWORD}
